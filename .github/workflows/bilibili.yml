on: [push, pull_request]

name: bilibili

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        deno: ["v1.5.x"]
        os: [ubuntu-latest]
    name: bilibili.com
    steps:
      - uses: actions/checkout@v2
      - name: Setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: ${{ matrix.deno }}

      - name: Test
        # run: deno test -A ./extractor/bilibili.test.ts
        run: deno --version
